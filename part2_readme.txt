Instruction to run the code:

Run the python script: part2_customer_segmentation.py using Python 3.x (it was written in Spyder 3.3.6 running Python 3.6).
Please change the path in order to point to where your CSV file is located, as at the moment it points to a local folder.

The final result is a plot of the customer data segmented using the k means algorithm with 4 clusters.